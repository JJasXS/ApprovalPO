# Local mkcert / OpenSSL TLS material (never commit)
.certs/
tls/

# Build outputs
artifacts/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/
publish/
out/

# Ad-hoc publish/test output (never commit; causes huge nested bin trees)
_lanbuild/
_routechk/
_verifybuild/
_audit_build/
_build_verify/
_bc*/
_bldchk*/
_out_*/

# Temp runtime files from local run scripts
.tmp_pid
.tmp_run.*

# Visual Studio / MSBuild
.vs/
*.user
*.userosscache
*.suo
*.cache
project.lock.json
project.fragment.lock.json

# JetBrains Rider
.idea/
*.sln.iml

# NuGet
*.nupkg
**/packages/*
!**/packages/build/

# Test / coverage
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*
*.trx
coverage*.json
coverage*.xml

# OS
.DS_Store
Thumbs.db

# Local secrets and env (keep *.example committed)
.env
.env.*.local

# OCR captures (private; served only when signed in)
Data/ocr-captures/
wwwroot/ocr-captures/
